"""Phase G — the catalog grows from production evidence. `docs/catalog_self_extension.md`.

**Automation layer** (plan §3.5). It sits under `fazerops.actions` so the layer-seam test's
existing prefix block covers it: nothing in the investigation layer can import a module that
decides what the catalog should become.
"""
