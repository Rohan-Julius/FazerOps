"""Writers authored by a model (W42 rung 3), merged by a human.

A module here is inert until a reviewer adds it to `writers.registry.WRITER_MODULES`. CI rejects
agent-authored commits that touch that list, and re-validates every module in this package on
every agent-authored commit (`growth/authoring.validate_module`).
"""
