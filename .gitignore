# Secrets — W1a. Nothing matching these may ever be tracked; tests/test_no_secrets.py enforces it.
.env
.env.*
!.env.example
*.pem
*.key
credentials
.aws/
*.kubeconfig
docs/
PLAN_FAZEROPS.md
CLAUDE.md

# Runtime state
.fazerops/
ledger.db
*.log

# Python
venv/
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
*.egg-info/
build/
dist/

# macOS
.DS_Store

# Media — backup takes are large and are uploaded, not committed
media/*.mp4
media/*.mov

# Third-party repos cloned for study — never vendored, never committed
.reference/

# k3d cluster state — audit logs and kubeconfigs, regenerated by scripts/setup_k3d.sh
.k3d/

# AgentCore starter toolkit local state (account id, absolute paths)
.bedrock_agentcore.yaml
.bedrock_agentcore/
